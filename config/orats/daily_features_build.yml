# ORATS daily-features build config (CLI overrides YAML)
logging:
  level: INFO
  color: true

paths:
  inter_root: data/intermediate/orats/api
  proc_root: data/processed/orats/daily_features

tickers:
  - SPY

endpoints:
  - dailies
  - summaries
  - hvs

prefix_endpoint_cols: true
priority_endpoints: null
collect_stats: true
columns: null
